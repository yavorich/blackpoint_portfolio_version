from itertools import chain

# from apps.account.endpoints import handlers as accounts_handlers
# from apps.common.endpoints import handlers as common_handlers


handlers = list(
    chain(
        # accounts_handlers,
        # common_handlers,
    )
)
